apiVersion: v1
kind: Service
metadata:
  labels:
    app: https-service
  name: https-service
spec:
  clusterIP: 10.101.166.126
  ports:
  - name: frontendport
    port: 88
    targetPort: backendport
  selector:
    app: https-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: https-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: https-service
  template:
    metadata:
      labels:
        app: https-service
    spec:
      nodeName: vfanny
      containers:
      - name: https-service
        securityContext:
          privileged: true
        image: localhost/https-service:v0.0.2
        ports:
        - containerPort: 8443
          name: backendport
        command: [ "./https_server" ]
